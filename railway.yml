version: 2
services:
  web:
    name: frontend
    dockerfile: Dockerfile
    buildPath: /app
    port: $PORT # Utiliza la variable de entorno PORT
    env: production
    routes:
      - src: /.*
        dest: /index.html
